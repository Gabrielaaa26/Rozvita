rules_version = '2';

// Allows read/write access on all documents to any user signed in to the application
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
